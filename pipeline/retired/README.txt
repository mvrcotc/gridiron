Superseded 2026-09-12 by project.py, which applies weather and injury changes to model INPUTS and re-simulates.
These scripts rescaled simulated percentiles after the fact, which turned whole-number receptions into fractions
and double-counted catch rate in receiving yards. Kept for reference only; refresh.py does not call them.
